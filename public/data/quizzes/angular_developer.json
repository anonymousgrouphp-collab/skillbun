[
  {
    "question": "A new developer is joining your team and asks about the primary purpose of Angular. Which of the following best describes what Angular is designed to build?",
    "options": [
      "Backend APIs and databases",
      "Cross-platform desktop applications",
      "Complex single-page applications (SPAs)",
      "Server-side rendered static websites"
    ],
    "correctIndex": 2,
    "explanation": "Angular is a powerful framework specifically designed for building complex, data-driven single-page applications (SPAs) that run in a web browser. While other options represent different types of applications, they are not Angular's primary focus.",
    "difficulty": "easy"
  },
  {
    "question": "You need to create a reusable UI block in your Angular application, such as a custom button or a navigation bar. Which Angular building block would you primarily use for this purpose?",
    "options": [
      "Service",
      "Module",
      "Component",
      "Directive"
    ],
    "correctIndex": 2,
    "explanation": "Components are the fundamental building blocks of Angular applications, responsible for controlling a part of the screen via a template and associated logic. Services are for shared logic, modules organize code, and directives modify existing DOM elements.",
    "difficulty": "easy"
  },
  {
    "question": "To quickly generate a new Angular component, service, or module from the command line, which essential Angular tool would a developer typically use?",
    "options": [
      "npm (Node Package Manager)",
      "Webpack",
      "Angular CLI (Command Line Interface)",
      "TypeScript Compiler (tsc)"
    ],
    "correctIndex": 2,
    "explanation": "The Angular CLI is the official tool for initializing, developing, scaffolding, and maintaining Angular applications. npm is a package manager, Webpack is a module bundler used by the CLI, and tsc compiles TypeScript.",
    "difficulty": "easy"
  },
  {
    "question": "You have an input field in your Angular template and want to update a component property `userName` automatically whenever the user types into it. Which type of data binding is most suitable for achieving this two-way synchronization?",
    "options": [
      "Event Binding `(event)`",
      "Property Binding `[property]`",
      "Interpolation `{{ value }}`",
      "Two-Way Binding `[(ngModel)]`"
    ],
    "correctIndex": 3,
    "explanation": "Two-way binding, typically achieved with `[(ngModel)]`, allows data to flow both from the component to the view and from the view to the component simultaneously, making it ideal for form input elements where changes need to be reflected immediately in the component's model.",
    "difficulty": "easy"
  },
  {
    "question": "Your Angular application needs to navigate users between different views (e.g., from /home to /dashboard) without a full page reload. Which core Angular feature enables this client-side navigation?",
    "options": [
      "HttpClient",
      "Angular Forms Module",
      "Angular Router",
      "Angular Services"
    ],
    "correctIndex": 2,
    "explanation": "The Angular Router is responsible for handling client-side navigation within an Angular application, allowing different component views to be displayed based on the URL path.",
    "difficulty": "easy"
  },
  {
    "question": "An Angular developer is defining the shape of an object that will be used across multiple components to ensure type safety. Which TypeScript feature is primarily used for this purpose?",
    "options": [
      "Class",
      "Interface",
      "Enum",
      "Decorator"
    ],
    "correctIndex": 1,
    "explanation": "Interfaces in TypeScript are used to define contracts for objects, specifying the names and types of properties that an object must have. They are crucial for ensuring type safety and code consistency across an application.",
    "difficulty": "easy"
  },
  {
    "question": "You have an `AuthService` that fetches user authentication status and want to make it available to multiple components without creating a new instance in each component. How would you typically provide this service in Angular to ensure a single, shared instance?",
    "options": [
      "Import `AuthService` directly into each component's constructor.",
      "Declare `AuthService` in the `providers` array of `AppModule` or a specific component.",
      "Add `AuthService` to the `declarations` array of `AppModule`.",
      "Define `AuthService` as a global JavaScript variable."
    ],
    "correctIndex": 1,
    "explanation": "Dependency Injection in Angular allows you to provide services as singletons (by default when provided in `AppModule` or with `providedIn: 'root'`) or scoped instances, making them available to components that declare them in their constructors. Declaring it in `providers` ensures Angular's injector handles its lifecycle.",
    "difficulty": "easy"
  },
  {
    "question": "Your Angular application needs to fetch data from an API, which is an asynchronous operation. To handle this, you typically use `HttpClient` methods that return an `Observable`. What is the primary benefit of using Observables for HTTP requests in Angular?",
    "options": [
      "They block the UI until the data is received.",
      "They allow for synchronous data processing.",
      "They provide a declarative way to handle asynchronous data streams, including error handling and retries.",
      "They are a simpler alternative to Promises with less functionality."
    ],
    "correctIndex": 2,
    "explanation": "Observables from RxJS provide a powerful, declarative paradigm for handling asynchronous operations like HTTP requests. They offer features for composing operations, managing multiple emissions, error handling, and retrying, which are superior to basic Promises for complex data streams.",
    "difficulty": "moderate"
  },
  {
    "question": "You are building a complex user registration form in Angular that requires extensive validation, dynamic fields, and testing. Which Angular forms approach is generally recommended for such scenarios due to its explicit and immutable nature?",
    "options": [
      "Template-driven forms",
      "Reactive forms",
      "HTML5 forms",
      "jQuery forms"
    ],
    "correctIndex": 1,
    "explanation": "Reactive forms provide a more robust and scalable approach for complex forms. They are built programmatically in the component class, offering better testability, explicit control over form state, and easier implementation of dynamic validation and field manipulation compared to template-driven forms.",
    "difficulty": "moderate"
  },
  {
    "question": "You want to add a special visual effect (e.g., highlighting) to an HTML element only when a certain condition is met in your component. Which type of Angular entity is best suited for directly manipulating the DOM behavior or appearance of an element based on conditions?",
    "options": [
      "Component",
      "Service",
      "Structural Directive (e.g., `*ngIf`)",
      "Attribute Directive (e.g., `ngClass`)"
    ],
    "correctIndex": 3,
    "explanation": "Attribute directives (like `ngClass` or `ngStyle`, or custom ones) are used to change the appearance or behavior of an existing DOM element. Structural directives (`*ngIf`, `*ngFor`) add or remove elements, and components manage their own views. Services encapsulate business logic.",
    "difficulty": "moderate"
  },
  {
    "question": "A developer has written an Angular service that fetches data from an API. Before pushing the code, they want to ensure that the service correctly calls the `HttpClient` and processes the response, independent of the actual backend. What kind of test should they write?",
    "options": [
      "End-to-end (E2E) test",
      "Integration test",
      "Unit test",
      "Acceptance test"
    ],
    "correctIndex": 2,
    "explanation": "A unit test focuses on testing a single, isolated unit of code (like a service function) in isolation, often using mocks or spies for its dependencies (like `HttpClient`) to control its behavior and ensure the unit's logic is correct.",
    "difficulty": "moderate"
  },
  {
    "question": "You need to perform an action in your Angular component (e.g., fetching initial data) immediately after Angular has initialized all data-bound properties of a directive or component. Which lifecycle hook is the most appropriate place to put this logic?",
    "options": [
      "`ngOnChanges`",
      "`ngOnInit`",
      "`ngDoCheck`",
      "`ngOnDestroy`"
    ],
    "correctIndex": 1,
    "explanation": "`ngOnInit` is a lifecycle hook that Angular calls once, after the component's data-bound properties have been initialized. It's the recommended place for initialization logic, such as fetching data from a server or setting up subscriptions.",
    "difficulty": "moderate"
  },
  {
    "question": "While developing an Angular application, you encounter an unexpected error in the browser console. To effectively pinpoint the source of the issue in your TypeScript code, which browser development tool feature is most useful?",
    "options": [
      "Network tab",
      "Elements tab",
      "Sources tab with breakpoints",
      "Console tab for logs only"
    ],
    "correctIndex": 2,
    "explanation": "The \"Sources\" tab in browser developer tools allows you to view your application's source code (including transpiled TypeScript), set breakpoints to pause execution, step through code, and inspect variables, which is crucial for detailed debugging.",
    "difficulty": "moderate"
  },
  {
    "question": "Your Angular enterprise application has grown very large, and initial load times are becoming slow. Many features are only accessed by a subset of users. What routing optimization technique can significantly improve the initial load performance by loading feature modules only when they are needed?",
    "options": [
      "Eager loading",
      "Preloading all modules",
      "Server-side rendering",
      "Lazy loading"
    ],
    "correctIndex": 3,
    "explanation": "Lazy loading allows you to load feature modules only when a user navigates to routes that belong to them. This reduces the initial bundle size, leading to faster application startup times, especially for large applications.",
    "difficulty": "moderate"
  },
  {
    "question": "Your team uses Git for version control. After working on a new feature, you need to integrate your changes into the main development branch while keeping a clean, linear history. Which Git command sequence would achieve this, assuming your feature branch is up-to-date with `main`?",
    "options": [
      "`git merge feature-branch` then `git push`",
      "`git rebase main` then `git merge feature-branch`",
      "`git checkout main`, `git pull`, `git rebase feature-branch`, `git push`",
      "`git checkout feature-branch`, `git rebase main`, `git checkout main`, `git merge feature-branch`, `git push`"
    ],
    "correctIndex": 3,
    "explanation": "To maintain a clean linear history, developers typically rebase their feature branch onto the target branch (`main`) to incorporate recent changes, then merge the rebased feature branch into `main` (often via a fast-forward merge if no new commits were made to `main` since the rebase), and finally push. This ensures the feature branch's commits appear after `main`'s latest history.",
    "difficulty": "moderate"
  },
  {
    "question": "Before deploying an Angular application to a production server, it's crucial to optimize it for performance and bundle size. Which Angular CLI command is specifically designed to build the application for production, including optimizations like Ahead-of-Time (AOT) compilation and tree-shaking?",
    "options": [
      "`ng serve`",
      "`ng test`",
      "`ng build --prod` (or `ng build --configuration production` in newer CLI)",
      "`ng lint`"
    ],
    "correctIndex": 2,
    "explanation": "The `ng build --prod` (or `ng build --configuration production`) command builds the application for production, applying various optimizations such as AOT compilation, minification, tree-shaking, and dead code elimination to produce small, highly performant bundles.",
    "difficulty": "moderate"
  },
  {
    "question": "You are fetching data using an `HttpClient.get()` Observable in Angular. If the HTTP request fails, you want to display an error message to the user and prevent the application from crashing. Which RxJS operator is best suited for catching and handling errors within an Observable stream?",
    "options": [
      "`map`",
      "`filter`",
      "`catchError`",
      "`tap`"
    ],
    "correctIndex": 2,
    "explanation": "The `catchError` RxJS operator is specifically designed to intercept errors in an observable stream. It allows you to provide an error-handling function that can log the error, transform it, or return a new observable to recover from the error gracefully.",
    "difficulty": "moderate"
  },
  {
    "question": "In a large Angular application, multiple components often need to share and react to the same piece of global application state (e.g., user authentication status or a shopping cart). Which architectural pattern or library is commonly used in Angular to manage this shared state in a predictable way?",
    "options": [
      "Local component state with `@Input()` and `@Output()`",
      "Direct DOM manipulation",
      "Redux-inspired patterns (e.g., NgRx, Akita)",
      "Storing data in `localStorage`"
    ],
    "correctIndex": 2,
    "explanation": "For complex applications with shared global state, Redux-inspired patterns and libraries like NgRx or Akita provide a structured, predictable, and scalable way to manage state. They centralize state, making changes traceable and debugging easier. While other options can manage some state, they don't offer the same level of predictability and scalability for global state.",
    "difficulty": "moderate"
  },
  {
    "question": "During a code review, an Angular component is flagged for poor accessibility. Specifically, a custom button component is not keyboard navigable and lacks proper screen reader announcements. Which Web Content Accessibility Guidelines (WCAG) principle is primarily being violated, and what should the developer implement?",
    "options": [
      "Perceivable; ensure sufficient color contrast.",
      "Robust; ensure compatibility with future technologies.",
      "Operable; ensure keyboard navigation and appropriate ARIA attributes.",
      "Understandable; ensure consistent navigation."
    ],
    "correctIndex": 2,
    "explanation": "The \"Operable\" WCAG principle requires that user interface components and navigation must be operable. This includes making sure interactive elements are keyboard accessible and that screen readers can properly interact with and announce their purpose and state, often achieved with appropriate ARIA attributes.",
    "difficulty": "moderate"
  },
  {
    "question": "As part of your team's development process, all new features or bug fixes must go through a peer review before being merged into the main branch. What mechanism in Git-based platforms (like GitHub, GitLab, Bitbucket) facilitates this review process?",
    "options": [
      "Direct commit to `main` branch",
      "Git tagging",
      "Pull Request (or Merge Request)",
      "Branching only without review"
    ],
    "correctIndex": 2,
    "explanation": "A Pull Request (or Merge Request in GitLab) is a request to merge changes from one branch into another. It serves as a formal review process, allowing team members to discuss, comment on, and approve code changes before they are integrated into the main codebase.",
    "difficulty": "moderate"
  },
  {
    "question": "An Angular application displays user-generated content, which includes HTML. A security audit reveals a potential Cross-Site Scripting (XSS) vulnerability. Which of the following Angular mechanisms is specifically designed to mitigate XSS risks when rendering untrusted HTML, and how should it be used?",
    "options": [
      "Using `innerHTML` directly with `DomSanitizer.bypassSecurityTrustHtml()` for all content.",
      "Escaping all user input on the backend before sending it to Angular.",
      "Leveraging Angular's built-in `DomSanitizer` to sanitize values bound to HTML properties like `innerHTML`.",
      "Using `*ngIf` to conditionally render the content."
    ],
    "correctIndex": 2,
    "explanation": "Angular's `DomSanitizer` automatically sanitizes values when they are inserted into the DOM via interpolation or property binding, protecting against XSS by stripping potentially dangerous code. While escaping on the backend is also good practice, Angular provides client-side protection. `bypassSecurityTrustHtml()` should be used with extreme caution and only for trusted content, not generally for user-generated content, as it explicitly disables sanitization.",
    "difficulty": "hard"
  },
  {
    "question": "You have an Angular component that receives a large, immutable data object via an `@Input()`. This component's template renders many sub-components. To optimize performance and minimize unnecessary re-rendering cycles when the input object reference hasn't changed, what change detection strategy should be applied?",
    "options": [
      "`Default` change detection",
      "`OnPush` change detection",
      "Manually calling `ChangeDetectorRef.detectChanges()` in `ngDoCheck`",
      "Using `ngZone.runOutsideAngular()`"
    ],
    "correctIndex": 1,
    "explanation": "The `OnPush` change detection strategy tells Angular to only check the component for changes when its input properties (references) change, when an event originates from the component itself, or when a manual change detection is triggered. This significantly improves performance for components that rely on immutable data or observables, preventing unnecessary checks of its subtree.",
    "difficulty": "hard"
  },
  {
    "question": "You need to test an Angular component that displays data fetched by a service. The test should verify that the component correctly renders the data and that the service is called with the right parameters, but without making actual HTTP requests. How would you set up your `TestBed` for this integration test?",
    "options": [
      "Provide the actual service in `TestBed.configureTestingModule` and let it make HTTP calls.",
      "Only declare the component in `TestBed`, ignoring the service.",
      "Provide a mock or spy object for the service within `TestBed.configureTestingModule` that simulates the service's behavior.",
      "Use `ngZone.runOutsideAngular` to skip service calls."
    ],
    "correctIndex": 2,
    "explanation": "For integration testing where a component interacts with a service, you typically provide a mock or spy object for the service in the `TestBed.configureTestingModule`. This allows you to control the service's behavior (e.g., make its methods return specific values or throw errors) without relying on actual external dependencies like HTTP requests, ensuring a focused and reliable test.",
    "difficulty": "hard"
  },
  {
    "question": "An Angular application exhibits poor runtime performance, with frequent UI freezes, especially during complex data manipulations. After profiling, it's identified that many synchronous operations are blocking the main thread. To improve responsiveness without completely refactoring the business logic, what JavaScript Web API or technique could be leveraged to offload heavy computations?",
    "options": [
      "Using `setTimeout` with a 0ms delay to defer execution.",
      "Employing `Web Workers` to run scripts in a background thread.",
      "Aggressively using Angular's `ngZone` to force change detection.",
      "Performing all computations directly within `ngDoCheck` lifecycle hook."
    ],
    "correctIndex": 1,
    "explanation": "Web Workers allow you to run scripts in background threads, separate from the main execution thread of the browser. This is ideal for offloading heavy, synchronous computations that would otherwise block the UI and lead to freezes, thereby improving the application's responsiveness.",
    "difficulty": "hard"
  },
  {
    "question": "A critical enterprise Angular application needs to protect certain routes from unauthorized access and also display different UI elements based on the user's roles. What Angular router feature is essential for enforcing authorization before navigating to a route, and what mechanism is commonly used to dynamically control UI element visibility based on roles?",
    "options": [
      "Route Guards for authorization; `*ngIf` with a service for UI elements.",
      "HTTP Interceptors for authorization; `@Input()` properties for UI elements.",
      "Lazy Loading for authorization; global CSS classes for UI elements.",
      "Custom Pipes for authorization; `ngAfterViewInit` for UI elements."
    ],
    "correctIndex": 0,
    "explanation": "Route Guards (like `CanActivate`) are the primary Angular mechanism for implementing authorization at the routing level, preventing users from accessing certain routes unless specific conditions (e.g., being logged in, having a specific role) are met. For dynamic UI element visibility based on roles, `*ngIf` combined with a service that provides user role information is a very common and effective approach.",
    "difficulty": "hard"
  },
  {
    "question": "What language is primarily used for Angular development?",
    "options": [
      "JavaScript",
      "TypeScript",
      "CoffeeScript",
      "Dart"
    ],
    "correctIndex": 1,
    "explanation": "Angular is built with and primarily uses TypeScript, a superset of JavaScript that adds static typing. TypeScript provides features like type checking, interfaces, decorators, and better tooling support that are essential to Angular's architecture.",
    "difficulty": "easy"
  },
  {
    "question": "What is a 'component' in Angular?",
    "options": [
      "A database connection",
      "A reusable building block of the UI that combines a TypeScript class, HTML template, and CSS styles",
      "A server-side script",
      "A testing utility"
    ],
    "correctIndex": 1,
    "explanation": "An Angular component is the fundamental building block of the UI. It consists of a TypeScript class (with a @Component decorator) containing logic, an HTML template defining the view, and optional CSS styles. Components are composable and reusable throughout the application.",
    "difficulty": "easy"
  },
  {
    "question": "What command is used to create a new Angular project using the Angular CLI?",
    "options": [
      "npm create angular",
      "ng new project-name",
      "angular init project-name",
      "create-angular-app project-name"
    ],
    "correctIndex": 1,
    "explanation": "The Angular CLI command 'ng new project-name' scaffolds a new Angular project with a default configuration, including necessary files, dependencies, and build tooling. The CLI provides commands for generating components, services, modules, and more.",
    "difficulty": "easy"
  },
  {
    "question": "What is Angular's 'NgModule' used for?",
    "options": [
      "To style components",
      "To organize the application into cohesive blocks of functionality by declaring components, directives, pipes, and importing other modules",
      "To make HTTP requests",
      "To handle routing exclusively"
    ],
    "correctIndex": 1,
    "explanation": "NgModule (decorated with @NgModule) is an organizational unit in Angular that groups related components, directives, pipes, and services. It declares what belongs to the module, imports other modules it depends on, and exports items for use by other modules.",
    "difficulty": "easy"
  },
  {
    "question": "What does 'data binding' mean in Angular?",
    "options": [
      "Connecting the database to the UI",
      "The mechanism that synchronizes data between the component's TypeScript class and its HTML template",
      "Binding CSS styles to elements",
      "Connecting two Angular applications together"
    ],
    "correctIndex": 1,
    "explanation": "Data binding in Angular synchronizes data between the component class and its template. Angular supports four types: interpolation ({{ }}), property binding ([property]), event binding ((event)), and two-way binding ([(ngModel)]), enabling dynamic and interactive UIs.",
    "difficulty": "easy"
  },
  {
    "question": "What is a 'service' in Angular?",
    "options": [
      "A web server that hosts the Angular app",
      "A class decorated with @Injectable that encapsulates reusable business logic, data access, or shared functionality",
      "A type of Angular component",
      "A CSS styling module"
    ],
    "correctIndex": 1,
    "explanation": "An Angular service is a class with the @Injectable decorator that provides reusable functionality separate from components — such as data fetching, logging, or business logic. Services are typically injected into components via Angular's dependency injection system.",
    "difficulty": "easy"
  },
  {
    "question": "What is the Angular CLI command to generate a new component?",
    "options": [
      "ng create component name",
      "ng generate component name",
      "ng add component name",
      "ng build component name"
    ],
    "correctIndex": 1,
    "explanation": "The command 'ng generate component name' (or shorthand 'ng g c name') creates a new component with its TypeScript class, HTML template, CSS styles, and test file. It also automatically declares the component in the nearest module.",
    "difficulty": "easy"
  },
  {
    "question": "What is Angular's 'dependency injection' system, and what advantage does it provide?",
    "options": [
      "A way to inject CSS into components",
      "A design pattern where the framework provides (injects) class dependencies instead of the class creating them, enabling loose coupling and testability",
      "A mechanism for injecting JavaScript into HTML",
      "A security feature that prevents code injection attacks"
    ],
    "correctIndex": 1,
    "explanation": "Angular's DI system creates and manages instances of services and provides them to components or other services that declare them as dependencies. This promotes loose coupling (components don't create their own dependencies), facilitates testing (mock dependencies can be injected), and enables singleton service patterns.",
    "difficulty": "moderate"
  },
  {
    "question": "In Angular, what is an 'Observable' and which library provides it?",
    "options": [
      "A synchronous data structure from lodash",
      "An asynchronous data stream from the RxJS library that emits values over time and supports operators for transformation",
      "A simple callback function from jQuery",
      "A static data type from TypeScript"
    ],
    "correctIndex": 1,
    "explanation": "Observables from RxJS (Reactive Extensions for JavaScript) represent asynchronous data streams that emit values over time. Angular uses Observables extensively for HTTP requests, form value changes, route changes, and event handling. They support powerful operators like map, filter, switchMap, and debounceTime.",
    "difficulty": "moderate"
  },
  {
    "question": "What is the difference between 'ngOnInit' and the 'constructor' in an Angular component?",
    "options": [
      "They are identical in purpose",
      "The constructor is for simple dependency injection; ngOnInit is for initialization logic that depends on input properties or requires DOM readiness",
      "ngOnInit runs before the constructor",
      "The constructor is deprecated"
    ],
    "correctIndex": 1,
    "explanation": "The constructor should only be used for dependency injection. ngOnInit (from the OnInit lifecycle hook) runs after Angular has set up the component's input properties, making it the appropriate place for initialization logic like fetching data, setting up subscriptions, or processing @Input values.",
    "difficulty": "moderate"
  },
  {
    "question": "What is Angular's 'Router' used for?",
    "options": [
      "Routing HTTP requests to a backend server",
      "Mapping URL paths to components, enabling single-page application navigation without full page reloads",
      "Routing data between services",
      "Managing network traffic"
    ],
    "correctIndex": 1,
    "explanation": "Angular Router enables client-side navigation in SPAs by mapping URL paths to specific components. It supports features like lazy loading, route guards, route parameters, child routes, and browser history management — all without full page reloads.",
    "difficulty": "moderate"
  },
  {
    "question": "What are Angular 'Pipes' used for?",
    "options": [
      "Connecting components together",
      "Transforming displayed data in templates without modifying the underlying data (e.g., formatting dates, currency, text)",
      "Creating data pipelines between services",
      "Managing state in the application"
    ],
    "correctIndex": 1,
    "explanation": "Pipes transform data for display in templates. Built-in pipes include DatePipe, CurrencyPipe, UpperCasePipe, AsyncPipe, etc. Custom pipes can be created for domain-specific transformations. They use the syntax {{ value | pipeName:arg }} and don't modify the original data.",
    "difficulty": "moderate"
  },
  {
    "question": "What is a 'Directive' in Angular, and what are the two main types?",
    "options": [
      "A type of component; structural and attribute",
      "An instruction that modifies the DOM; structural directives (*ngIf, *ngFor) change DOM structure, attribute directives (ngClass, ngStyle) change element appearance/behavior",
      "A database query; read and write",
      "A routing instruction; forward and redirect"
    ],
    "correctIndex": 1,
    "explanation": "Directives are instructions that modify the DOM. Structural directives (prefixed with *, like *ngIf and *ngFor) add/remove DOM elements. Attribute directives (like ngClass, ngStyle, or custom ones) change the appearance or behavior of existing elements without altering the DOM structure.",
    "difficulty": "moderate"
  },
  {
    "question": "What is 'lazy loading' in Angular routing, and why is it beneficial?",
    "options": [
      "Loading images slowly for visual effect",
      "Loading feature modules only when the user navigates to their route, reducing the initial bundle size and improving startup time",
      "Delaying all API calls until needed",
      "A debugging technique that slows execution"
    ],
    "correctIndex": 1,
    "explanation": "Lazy loading defers loading of feature modules until the user navigates to their associated routes. This splits the application into smaller bundles, reducing the initial download size and improving startup time. It's configured using loadChildren in route definitions.",
    "difficulty": "moderate"
  },
  {
    "question": "In Angular, what is the purpose of 'Route Guards'?",
    "options": [
      "To protect routes from XSS attacks",
      "To control navigation to and from routes based on conditions like authentication status or unsaved changes",
      "To guard against CSS style conflicts",
      "To prevent multiple users from accessing the same route"
    ],
    "correctIndex": 1,
    "explanation": "Route Guards are interfaces (CanActivate, CanDeactivate, CanLoad, etc.) that implement logic to control navigation. For example, CanActivate can redirect unauthenticated users to a login page, and CanDeactivate can prompt users about unsaved changes before leaving a form.",
    "difficulty": "moderate"
  },
  {
    "question": "What is Angular's 'HttpClient' module used for?",
    "options": [
      "Creating HTTP servers",
      "Making HTTP requests to backend APIs with features like typed responses, interceptors, and Observable-based responses",
      "Serving static files",
      "Managing WebSocket connections exclusively"
    ],
    "correctIndex": 1,
    "explanation": "HttpClient (from @angular/common/http) provides a simplified API for making HTTP requests. It returns Observables, supports typed responses (generics), enables request/response interception via HttpInterceptors, and provides built-in error handling and retry capabilities.",
    "difficulty": "moderate"
  },
  {
    "question": "What is the purpose of 'Reactive Forms' in Angular compared to Template-Driven Forms?",
    "options": [
      "Reactive Forms are slower but simpler",
      "Reactive Forms define form structure programmatically in the component class using FormGroup/FormControl, offering more control, testability, and scalability",
      "They are identical in functionality",
      "Template-Driven Forms are always preferred"
    ],
    "correctIndex": 1,
    "explanation": "Reactive Forms define form structure in the component class using FormGroup, FormControl, and FormArray. They provide synchronous access to the form model, easier unit testing, dynamic form creation, and custom validators. Template-Driven Forms use directives in the template and are simpler but less flexible.",
    "difficulty": "moderate"
  },
  {
    "question": "What is an 'HttpInterceptor' in Angular?",
    "options": [
      "A firewall for the application",
      "A middleware that intercepts outgoing HTTP requests and incoming responses to modify them globally (e.g., adding auth headers, logging, error handling)",
      "A component that displays HTTP errors",
      "A tool for intercepting user clicks"
    ],
    "correctIndex": 1,
    "explanation": "HttpInterceptors are classes implementing the HttpInterceptor interface that sit between the application and the HttpClient. They can modify requests (e.g., adding authentication tokens), handle responses (e.g., refreshing tokens on 401 errors), and perform cross-cutting concerns like logging.",
    "difficulty": "moderate"
  },
  {
    "question": "In Angular's change detection, what is the difference between 'Default' and 'OnPush' strategies?",
    "options": [
      "There is no difference",
      "Default checks the entire component tree on every event; OnPush only checks a component when its @Input references change, an event is emitted within it, or change detection is manually triggered",
      "OnPush is slower than Default",
      "Default is deprecated in favor of OnPush"
    ],
    "correctIndex": 1,
    "explanation": "Default change detection runs on every browser event (click, timer, HTTP response) and checks the entire component subtree. OnPush optimizes performance by only running change detection when @Input object references change, an event originates within the component, or detectChanges() is called manually.",
    "difficulty": "moderate"
  },
  {
    "question": "What are Angular 'Signals' introduced in Angular 16+?",
    "options": [
      "A messaging system between microservices",
      "A reactive primitive that wraps a value and notifies consumers when that value changes, enabling fine-grained reactivity",
      "A logging system for debugging",
      "A replacement for TypeScript"
    ],
    "correctIndex": 1,
    "explanation": "Angular Signals provide a fine-grained reactive primitive. A signal wraps a value and tracks which parts of the application depend on it. When the signal's value changes, only the dependent views update, enabling more efficient change detection compared to zone.js-based detection.",
    "difficulty": "moderate"
  },
  {
    "question": "An Angular application has a complex form with dynamic fields, cross-field validation, and conditional sections. How should this be architected for maintainability and testability?",
    "options": [
      "Using Template-Driven Forms with ngModel for each field",
      "Using Reactive Forms with FormGroup, dynamic FormArray for repeated sections, custom validators for cross-field rules, and separate form-builder services for testability",
      "Handling all validation with HTML5 attributes",
      "Using plain JavaScript DOM manipulation"
    ],
    "correctIndex": 1,
    "explanation": "Reactive Forms provide the programmatic control needed: FormGroup for the overall form, FormArray for dynamic repeated sections, custom ValidatorFn for cross-field validation rules, and the form model can be constructed in a testable service. This approach is fully unit-testable without any DOM dependency.",
    "difficulty": "hard"
  },
  {
    "question": "In a large Angular enterprise application, what is the recommended approach for managing global state across multiple feature modules?",
    "options": [
      "Using local component state everywhere",
      "Implementing a centralized state management pattern using NgRx (Redux pattern) with Store, Actions, Reducers, Effects, and Selectors",
      "Passing data through deeply nested @Input/@Output chains",
      "Storing all state in localStorage"
    ],
    "correctIndex": 1,
    "explanation": "NgRx implements the Redux pattern for Angular: a single Store holds application state, Actions describe state changes, Reducers specify how state transitions occur, Effects handle side effects (API calls), and Selectors efficiently derive computed state. This provides predictable state management, powerful debugging (time-travel), and testability.",
    "difficulty": "hard"
  },
  {
    "question": "An Angular application's bundle size is 2MB and initial load time exceeds 5 seconds. Which combination of techniques would most effectively reduce this?",
    "options": [
      "Adding more polyfills",
      "Lazy loading feature modules, enabling tree-shaking and Ahead-of-Time (AOT) compilation, and using the Angular build optimizer",
      "Adding more components to the AppModule",
      "Disabling TypeScript compilation"
    ],
    "correctIndex": 1,
    "explanation": "The combined approach addresses bundle size from multiple angles: lazy loading splits the bundle so only essential code loads initially; tree-shaking removes unused code; AOT compilation eliminates the template compiler from the runtime bundle; and the build optimizer further reduces code through advanced minification and dead code elimination.",
    "difficulty": "hard"
  },
  {
    "question": "When using RxJS in Angular, a developer subscribes to an Observable in a component but forgets to unsubscribe. What is the consequence, and what is the recommended pattern to prevent it?",
    "options": [
      "Nothing happens; Angular handles cleanup automatically",
      "Memory leaks and potential runtime errors after component destruction; use the async pipe in templates, takeUntilDestroyed(), or explicit unsubscribe in ngOnDestroy",
      "The application crashes immediately",
      "The subscription automatically stops after 30 seconds"
    ],
    "correctIndex": 1,
    "explanation": "Unsubscribed Observables continue to exist after component destruction, causing memory leaks and potentially triggering logic on destroyed components. Best practices include: using the async pipe (auto-unsubscribes), takeUntilDestroyed() from @angular/core, or manually unsubscribing in ngOnDestroy via a Subject/takeUntil pattern.",
    "difficulty": "hard"
  },
  {
    "question": "Angular supports standalone components (introduced in Angular 14+). How do standalone components differ from traditional NgModule-declared components, and what architectural advantage do they provide?",
    "options": [
      "Standalone components cannot use services",
      "Standalone components declare their own imports directly in the @Component decorator, eliminating the need for NgModules and simplifying the dependency graph",
      "Standalone components are slower than NgModule components",
      "They are only usable in test environments"
    ],
    "correctIndex": 1,
    "explanation": "Standalone components (standalone: true in @Component) manage their own dependencies by importing other components, directives, and pipes directly in their decorator — no NgModule required. This simplifies the application architecture, reduces boilerplate, enables easier tree-shaking, and makes lazy loading at the component level straightforward.",
    "difficulty": "hard"
  }
]
