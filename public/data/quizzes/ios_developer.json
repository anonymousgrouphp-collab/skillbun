[
  {
    "question": "A new iOS developer wants to declare a constant that stores the name of the app. Which Swift keyword should they use?",
    "options": [
      "var",
      "let",
      "static",
      "const"
    ],
    "correctIndex": 1,
    "explanation": "`let` is used to declare constants in Swift, meaning their value cannot be changed after initialization. `var` is for variables, `static` for type properties/methods, and `const` is not a Swift keyword.",
    "difficulty": "easy"
  },
  {
    "question": "While developing a SwiftUI view, an iOS developer wants to see live updates of their UI without running the app on a simulator or device. Which Xcode feature provides this capability?",
    "options": [
      "Breakpoints",
      "Instruments",
      "Previews",
      "Scheme Editor"
    ],
    "correctIndex": 2,
    "explanation": "SwiftUI Previews allow developers to see real-time renderings of their UI code within Xcode, facilitating rapid iteration. Breakpoints are for debugging, Instruments for performance analysis, and Scheme Editor for build/run configurations.",
    "difficulty": "easy"
  },
  {
    "question": "To arrange three Text views vertically in a SwiftUI interface, what is the most appropriate layout container to use?",
    "options": [
      "HStack",
      "ZStack",
      "VStack",
      "Grid"
    ],
    "correctIndex": 2,
    "explanation": "`VStack` arranges its child views in a vertical line. `HStack` arranges views horizontally, `ZStack` layers views from back to front, and `Grid` is for more complex grid layouts.",
    "difficulty": "easy"
  },
  {
    "question": "An iOS developer wants to create a simple toggle switch in SwiftUI where its `isOn` state is managed directly within the view itself. Which property wrapper is suitable for this purpose?",
    "options": [
      "@Binding",
      "@EnvironmentObject",
      "@Published",
      "@State"
    ],
    "correctIndex": 3,
    "explanation": "`@State` is used for managing simple, local value types within a SwiftUI view, making it the ideal choice for a view's own internal state like a toggle's `isOn` property. `@Binding` is for passing state to child views, `@EnvironmentObject` for shared state across a view hierarchy, and `@Published` is used with `ObservableObject` classes.",
    "difficulty": "easy"
  },
  {
    "question": "A function needs to return a `String`, but in some error cases, it might not be able to produce a valid string. How should the return type be declared to indicate that a `String` might be absent?",
    "options": [
      "String!",
      "String?",
      "String??",
      "Optional<String>"
    ],
    "correctIndex": 1,
    "explanation": "`String?` (or `Optional<String>`) declares an optional type, indicating that the variable or return value might contain a `String` or `nil`. `String!` is an implicitly unwrapped optional, and `String??` is a double optional which is rarely used in this context.",
    "difficulty": "easy"
  },
  {
    "question": "For storing a small piece of user preference data, such as whether a user has seen an onboarding tutorial, which built-in iOS persistence mechanism is most suitable?",
    "options": [
      "Core Data",
      "SwiftData",
      "UserDefaults",
      "File System"
    ],
    "correctIndex": 2,
    "explanation": "`UserDefaults` is ideal for storing small amounts of user preferences, settings, and other app-specific data. Core Data and SwiftData are for more complex object graphs and larger datasets, while the File System is for arbitrary files.",
    "difficulty": "easy"
  },
  {
    "question": "Before submitting an app to the App Store for public release, an iOS developer wants to distribute beta versions to a select group of testers to gather feedback. Which Apple service is designed for this purpose?",
    "options": [
      "App Store Connect",
      "Developer Portal",
      "TestFlight",
      "Xcode Cloud"
    ],
    "correctIndex": 2,
    "explanation": "TestFlight is Apple's service for beta testing iOS apps before their official release on the App Store. App Store Connect is for managing app submissions, Developer Portal for certificates/identifiers, and Xcode Cloud for CI/CD.",
    "difficulty": "easy"
  },
  {
    "question": "An iOS developer is designing a `Point` data type with `x` and `y` coordinates. Since points are typically small, value-based entities that are copied when assigned and passed, which Swift type is generally preferred?",
    "options": [
      "class",
      "enum",
      "struct",
      "protocol"
    ],
    "correctIndex": 2,
    "explanation": "`struct`s are value types in Swift, meaning they are copied when assigned or passed, which is suitable for small, independent data models like `Point`. `class`es are reference types. `enum`s are for distinct cases, and `protocol`s define blueprints of methods/properties.",
    "difficulty": "moderate"
  },
  {
    "question": "To allow users to navigate from a list of items to a detail view for a selected item in SwiftUI, which view container and navigation component combination is typically used?",
    "options": [
      "VStack with Link",
      "HStack with Button",
      "NavigationView (or NavigationStack/NavigationSplitView) with NavigationLink",
      "ZStack with Toggle"
    ],
    "correctIndex": 2,
    "explanation": "For hierarchical navigation, `NavigationView` (or the newer `NavigationStack`/`NavigationSplitView` in iOS 16+) combined with `NavigationLink` is the standard pattern in SwiftUI to push new views onto a navigation stack. `Link` is for opening URLs, `Button` for actions, and `Toggle` for boolean states.",
    "difficulty": "moderate"
  },
  {
    "question": "An iOS developer wants to share a complex data model, `UserProfile`, across multiple views in a SwiftUI application without explicitly passing it down through every initializer. Which SwiftUI property wrapper and object type combination is best suited for this global-like state management?",
    "options": [
      "@State with struct",
      "@Binding with class",
      "@EnvironmentObject with ObservableObject",
      "@Published with enum"
    ],
    "correctIndex": 2,
    "explanation": "`@EnvironmentObject` is designed to inject an `ObservableObject` instance into a view hierarchy from an ancestor, making it accessible to any descendant view without manual passing. `@State` is for local value types, `@Binding` for passing state to child views, and `@Published` is a property wrapper *within* an `ObservableObject`.",
    "difficulty": "moderate"
  },
  {
    "question": "An app needs to fetch JSON data from a remote API and display it. The JSON response directly maps to a Swift `struct`. Which Swift protocol should the `struct` conform to to easily convert the JSON data into a Swift object using `JSONDecoder`?",
    "options": [
      "Hashable",
      "Comparable",
      "Codable",
      "Identifiable"
    ],
    "correctIndex": 2,
    "explanation": "`Codable` is a type alias for both `Encodable` and `Decodable` protocols. Conforming to `Codable` allows Swift types to be easily converted to and from external representations like JSON using `JSONEncoder` and `JSONDecoder`.",
    "difficulty": "moderate"
  },
  {
    "question": "To define a persistent model for an entity named `Task` with properties `title` (String) and `isComplete` (Bool) using SwiftData, which attribute should be applied to the `Task` class?",
    "options": [
      "@Model",
      "@Persistent",
      "@DataStore",
      "@CoreData"
    ],
    "correctIndex": 0,
    "explanation": "In SwiftData, the `@Model` macro is used to designate a class as a persistent model, automatically generating the necessary boilerplate for storing and retrieving its instances. The other options are not standard SwiftData attributes.",
    "difficulty": "moderate"
  },
  {
    "question": "When writing a unit test for a function that fetches data from a network, it's often desirable to isolate the function from actual network calls to ensure test reliability and speed. What technique is commonly used to achieve this isolation?",
    "options": [
      "UI testing",
      "Integration testing",
      "Mocking",
      "End-to-end testing"
    ],
    "correctIndex": 2,
    "explanation": "Mocking involves creating simulated versions of dependencies (like network services) that return predictable data, allowing the unit under test to be tested in isolation without relying on external systems. UI testing tests the user interface, Integration testing tests interaction between components, and End-to-end testing tests the entire system.",
    "difficulty": "moderate"
  },
  {
    "question": "An iOS development team is building a complex feature where view logic is kept separate from data management logic, and a dedicated `ViewModel` acts as an intermediary to provide data to the `View` and handle its actions. Which architectural pattern are they likely implementing?",
    "options": [
      "MVC (Model-View-Controller)",
      "MVVM (Model-View-ViewModel)",
      "VIPER",
      "Redux"
    ],
    "correctIndex": 1,
    "explanation": "MVVM is a popular architectural pattern in iOS development, especially with SwiftUI, where the `ViewModel` abstracts the data and business logic from the `View`, making the view more declarative and testable. MVC is the traditional Apple pattern, VIPER is more rigid, and Redux is a state management pattern.",
    "difficulty": "moderate"
  },
  {
    "question": "An app features a custom `Button` with only an icon and no visible text. To make this button accessible for users with visual impairments using VoiceOver, what is the most important property to set?",
    "options": [
      "tintColor",
      "alpha",
      "accessibilityLabel",
      "isUserInteractionEnabled"
    ],
    "correctIndex": 2,
    "explanation": "`accessibilityLabel` provides a concise, localized description of the UI element's purpose to accessibility technologies like VoiceOver, allowing users to understand the button's action even without visual text. Other properties relate to visual appearance or interaction.",
    "difficulty": "moderate"
  },
  {
    "question": "An iOS app is crashing intermittently, and the developer suspects a memory leak. Which Xcode tool is specifically designed to help identify and analyze memory-related issues and performance bottlenecks?",
    "options": [
      "Breakpoints",
      "LLDB",
      "Instruments",
      "Debug View Hierarchy"
    ],
    "correctIndex": 2,
    "explanation": "Instruments is a powerful profiling and analysis tool provided by Xcode that includes templates like Leaks, Allocations, and Time Profiler, which are crucial for identifying memory leaks and performance bottlenecks. Breakpoints and LLDB are for code execution debugging, and Debug View Hierarchy is for UI layout issues.",
    "difficulty": "moderate"
  },
  {
    "question": "A Swift function `func loadData() throws -> Data` can throw an error. How would an iOS developer safely call this function and handle potential errors?",
    "options": [
      "try loadData()",
      "loadData()!",
      "do { try loadData() } catch { ... }",
      "loadData() ?? fallbackData"
    ],
    "correctIndex": 2,
    "explanation": "The `do-catch` statement is the standard Swift mechanism for handling errors thrown by functions marked with `throws`. `try` is required but must be within a `do-catch` or a throwing function. `!` forcibly unwraps (unsafe), and `??` is for optionals, not thrown errors.",
    "difficulty": "moderate"
  },
  {
    "question": "When building an offline-first feature, an iOS developer needs to ensure that local changes made by the user are synchronized with a remote server when connectivity is restored. Which strategy is generally recommended for handling this synchronization reliably?",
    "options": [
      "Always pull data on app launch",
      "Use a background fetch every 5 minutes",
      "Implement a local queue for changes and sync it when online",
      "Only allow online operations"
    ],
    "correctIndex": 2,
    "explanation": "An effective offline-first strategy involves queuing local user changes (e.g., using a Core Data store or custom persistence) and then intelligently synchronizing these changes with the remote server once an internet connection is available, often with conflict resolution.",
    "difficulty": "moderate"
  },
  {
    "question": "As of recent iOS versions, apps submitted to the App Store must provide transparency about their data collection practices. Where does an iOS developer declare the types of data their app collects and how it's used for the App Store listing?",
    "options": [
      "Info.plist",
      "Project.xcodeproj",
      "Privacy Nutrition Labels in App Store Connect",
      "AppDelegate.swift"
    ],
    "correctIndex": 2,
    "explanation": "Developers must complete the \"Privacy Nutrition Labels\" section in App Store Connect, specifying data collection practices, which then appear on the app's product page. While `Info.plist` contains privacy-related usage descriptions, the aggregate data collection details are in App Store Connect.",
    "difficulty": "moderate"
  },
  {
    "question": "An iOS developer wants to add a new method `squared()` to all numeric types (e.g., `Int`, `Double`, `Float`) in their project without modifying their original definitions. What Swift language feature can achieve this efficiently?",
    "options": [
      "Inheritance",
      "Generics",
      "Protocol Extensions",
      "Type Aliases"
    ],
    "correctIndex": 2,
    "explanation": "Protocol extensions allow you to add new methods and properties to types that conform to a specific protocol. By extending a protocol like `Numeric`, you can add functionality to all types that conform to it (like `Int`, `Double`), without subclassing or modifying original definitions.",
    "difficulty": "moderate"
  },
  {
    "question": "An iOS developer is building a custom reusable `Toggle` component. This component needs to receive a `Binding<Bool>` from its parent view, but also manage some internal, temporary `Bool` state that should *not* propagate back to the parent. How can this be achieved while keeping the external `Binding` functional?",
    "options": [
      "Use @State for both internal and external state",
      "Pass the Binding as a regular property",
      "Use @Binding for the external state and @State for the internal temporary state within the component",
      "Create an ObservableObject for the internal state"
    ],
    "correctIndex": 2,
    "explanation": "The external state that needs to propagate changes to the parent should use `@Binding`. For internal, temporary state that the component manages locally and shouldn't affect the parent directly, `@State` is the correct choice. Combining both allows for robust, reusable components.",
    "difficulty": "hard"
  },
  {
    "question": "When handling network requests, an iOS app needs to perform specific actions based on different HTTP status codes (e.g., refresh an authentication token for a 401 Unauthorized, display a user-friendly message for a 404 Not Found, retry for a 500 Internal Server Error). What is the most robust way to implement this logic after a `URLSession` data task completes?",
    "options": [
      "Check response.statusCode in a switch statement after try? data decoding",
      "Decode a custom error Codable type for every response",
      "Use a do-catch block and check URLError cases",
      "Implement a custom HTTPError enum that throws and contains associated values for specific status codes, then handle it in a do-catch"
    ],
    "correctIndex": 3,
    "explanation": "The most robust approach involves creating a custom `Error` enum that captures specific HTTP status codes and other relevant network failures. The networking layer can then `throw` instances of this custom error type, allowing calling code to handle specific error cases in a structured `do-catch` block, enabling precise recovery or user feedback.",
    "difficulty": "hard"
  },
  {
    "question": "An existing SwiftData app needs to add a new non-optional property to an `Item` model (e.g., `priority: Int`) that wasn't present in previous versions. Users already have existing `Item` data stored. How would an iOS developer handle this schema change gracefully without losing user data?",
    "options": [
      "Delete and recreate the database on app launch",
      "Mark the new property as optional",
      "Implement a SchemaMigrationPlan with MigrationStages",
      "Add the property and let SwiftData handle it automatically"
    ],
    "correctIndex": 2,
    "explanation": "For non-optional new properties or other significant schema changes, SwiftData requires an explicit `SchemaMigrationPlan`. This plan defines `MigrationStage`s where you can provide custom logic to transform existing data to conform to the new schema, preventing data loss. Simply adding the property would lead to crashes for existing users unless it's optional with a default.",
    "difficulty": "hard"
  },
  {
    "question": "A `UserService` class depends on an `APIManager` protocol to fetch user data. In a unit test for `UserService`, the goal is to verify that `fetchUser(id:)` correctly handles a network error returned by the `APIManager`. How should the `APIManager` dependency be configured for this test?",
    "options": [
      "Directly call the real APIManager instance",
      "Create a mock APIManager that always returns success",
      "Create a mock APIManager that throws a specific network error",
      "Use a spy APIManager to record interactions"
    ],
    "correctIndex": 2,
    "explanation": "To specifically test error handling, a mock `APIManager` should be created that is designed to fail predictably by throwing a predefined network error. This allows the test to isolate the `UserService`'s error-handling logic and verify its correctness without relying on actual network conditions.",
    "difficulty": "hard"
  },
  {
    "question": "An iOS app displays a list of `Product` objects. Some `Product` data comes from a local cache (SwiftData), and other `Product` data (e.g., real-time stock availability) comes from a network API. The app needs to show a unified, up-to-date list, prioritizing network data when available. What is an effective architectural approach to combine and present this data?",
    "options": [
      "Always display only cached data",
      "Fetch all data from the network and ignore cache",
      "Implement a repository pattern that abstracts data sources and resolves conflicts/priorities",
      "Use two separate lists side-by-side"
    ],
    "correctIndex": 2,
    "explanation": "A repository pattern is highly effective for abstracting multiple data sources (e.g., network, local persistence). The repository acts as a single source of truth, managing fetching, caching, and merging data from different origins, and resolving any conflicts or prioritizing network data, providing a unified interface to the rest of the application.",
    "difficulty": "hard"
  },
  {
    "question": "What is the primary programming language used to build modern iOS applications?",
    "options": [
      "Objective-C",
      "Swift",
      "Java",
      "Kotlin"
    ],
    "correctIndex": 1,
    "explanation": "Swift is Apple's modern, safe, and fast programming language, introduced in 2014 to replace Objective-C for iOS, macOS, watchOS, and tvOS development.",
    "difficulty": "easy"
  },
  {
    "question": "Which IDE is the official tool developed by Apple to build iOS applications?",
    "options": [
      "Android Studio",
      "Xcode",
      "Visual Studio",
      "IntelliJ IDEA"
    ],
    "correctIndex": 1,
    "explanation": "Xcode is Apple's integrated development environment (IDE) for macOS, containing code editors, compilers, and simulator tools for iOS development.",
    "difficulty": "easy"
  },
  {
    "question": "What is SwiftUI?",
    "options": [
      "A database engine developed by Apple",
      "Apple's modern declarative UI framework for building user interfaces across all Apple platforms",
      "A tool for compressing image file assets",
      "A programming language replacing Swift"
    ],
    "correctIndex": 1,
    "explanation": "SwiftUI allows developers to describe user interfaces declaratively using simple Swift code, replacing legacy Storyboard layouts.",
    "difficulty": "easy"
  },
  {
    "question": "What is the role of legacy UIKit in iOS development?",
    "options": [
      "An encryption standard for iOS databases",
      "Apple's classic imperative UI framework using views and view controllers to construct interfaces",
      "A package manager replacing CocoaPods",
      "A network routing protocol for Apple servers"
    ],
    "correctIndex": 1,
    "explanation": "UIKit is the older, imperative framework. While modern apps target SwiftUI, many existing apps and complex components still rely on UIKit code.",
    "difficulty": "easy"
  },
  {
    "question": "What does a ViewController do in UIKit architectures?",
    "options": [
      "Controls the physical hardware buttons of the iPhone",
      "Acts as the glue between the data model and the user interface view, managing screen lifecycles and interactions",
      "Controls the compiler settings in Xcode",
      "Manages database sync operations"
    ],
    "correctIndex": 1,
    "explanation": "In MVC architectures, the UIViewController manages view loading, user input events, and coordinates updates between data and the UI layout.",
    "difficulty": "easy"
  },
  {
    "question": "What does 'CocoaPods' or 'Swift Package Manager' (SPM) manage in iOS projects?",
    "options": [
      "The compilation timeline of binaries",
      "Third-party libraries and code dependencies imported into the project",
      "User login credentials securely",
      "Database schemas on server disks"
    ],
    "correctIndex": 1,
    "explanation": "CocoaPods and SPM are dependency managers, helping iOS developers download and update external packages (like Alamofire or Kingfisher).",
    "difficulty": "easy"
  },
  {
    "question": "What is an IPA file in iOS distribution?",
    "options": [
      "An internet protocol address",
      "The compiled application package file that installs the app on iOS devices",
      "An image asset folder format",
      "A database backup file"
    ],
    "correctIndex": 1,
    "explanation": "An .ipa file is the final binary package containing the compiled app, assets, and provision profiles, uploaded to App Store Connect.",
    "difficulty": "easy"
  },
  {
    "question": "What is the difference between value types (structs) and reference types (classes) in Swift?",
    "options": [
      "Structs are stored on the heap; classes on the stack",
      "Structs are copied when assigned or passed (value semantics); Classes are referenced, sharing the same instance when passed (reference semantics)",
      "Classes are always faster than Structs",
      "Structs do not support defining methods"
    ],
    "correctIndex": 1,
    "explanation": "In Swift, structs are lightweight value types (allocated on the stack). Classes are reference types (allocated on the heap), supporting inheritance and deinitializers.",
    "difficulty": "moderate"
  },
  {
    "question": "What is ARC (Automatic Reference Counting) in iOS memory management?",
    "options": [
      "A tool that counts the number of pages in the app",
      "Apple's compile-time memory management system that automatically tracks and manages class instance references to free heap memory",
      "A database cleanup scheduler",
      "A multithreading library in Swift"
    ],
    "correctIndex": 1,
    "explanation": "ARC inserts retain and release calls during compilation. When a class instance's reference count drops to zero, the runtime deallocates its memory.",
    "difficulty": "moderate"
  },
  {
    "question": "What is a 'Retain Cycle' (Strong Reference Cycle) and how is it resolved in Swift?",
    "options": [
      "A loop that reloads a screen repeatedly; resolved by navigation resets",
      "A memory leak occurring when two class instances hold strong references to each other, preventing ARC from releasing them; resolved by using weak or unowned references",
      "A database sync deadlock; resolved by locking tables",
      "A compiler bug; resolved by cleaning build folder"
    ],
    "correctIndex": 1,
    "explanation": "If Class A references Class B strongly, and Class B references Class A strongly, neither count hits zero. Resolving this requires using `weak` or `unowned` markers to break the cycle.",
    "difficulty": "moderate"
  },
  {
    "question": "What is GCD (Grand Central Dispatch) in iOS?",
    "options": [
      "Apple's main retail store headquarters",
      "A low-level API managing concurrent operations and dispatching tasks to system queue pools",
      "A database synchronization service",
      "A security standard for user authentication"
    ],
    "correctIndex": 1,
    "explanation": "GCD (using `DispatchQueue`) allows developers to offload tasks to background threads (e.g. download data on a background queue) and return to the main thread to update the UI.",
    "difficulty": "moderate"
  },
  {
    "question": "Why must all user interface updates occur on the Main Thread (Main Queue) in iOS?",
    "options": [
      "Because the main thread is the only thread that can access databases",
      "Because UIKit/SwiftUI is not thread-safe; updating UI from background threads causes glitches, lag, or application crashes",
      "Because background threads are disabled on iPhones",
      "Because the main thread has more CPU power than background threads"
    ],
    "correctIndex": 1,
    "explanation": "Rendering and window events run sequentially on the main thread. Accessing UI elements from parallel threads triggers race conditions in graphics buffers, causing crashes.",
    "difficulty": "moderate"
  },
  {
    "question": "What is a 'Closure' in Swift?",
    "options": [
      "A class destructor function",
      "A self-contained block of functionality that can be passed around and used in your code, capturing variables from its enclosing scope",
      "A tag that closes code blocks in Swift compilation",
      "An encryption algorithm for local files"
    ],
    "correctIndex": 1,
    "explanation": "Swift closures are equivalent to lambdas in other languages, commonly used as completion handlers for asynchronous operations.",
    "difficulty": "moderate"
  },
  {
    "question": "What is 'Auto Layout' in UIKit?",
    "options": [
      "An editor setting that auto-saves code files",
      "A constraint-based layout system that dynamically calculates the size and position of all views based on relationships, adapting to screen orientations",
      "An automated script that generates sitemaps",
      "A template for styling UI colors"
    ],
    "correctIndex": 1,
    "explanation": "Auto Layout uses mathematical constraints (e.g., 'Button center matches Screen center') to size and position elements across diverse device sizes.",
    "difficulty": "moderate"
  },
  {
    "question": "What does the 'App Delegate' (or Scene Delegate) do in iOS architectures?",
    "options": [
      "Manages the developer's Apple Developer account portal",
      "Acts as the orchestrator of application-level events, managing startup, background states, and scene windows",
      "Compiles Swift files into binaries",
      "Tracks app purchases and bills users"
    ],
    "correctIndex": 1,
    "explanation": "The App/Scene Delegate intercepts lifecycle events, notifying the app when it is launched, entering the background, or terminated by the OS.",
    "difficulty": "moderate"
  },
  {
    "question": "What is 'Core Data' in iOS development?",
    "options": [
      "Apple's cloud database storage engine",
      "A powerful object graph and persistence framework used to store data locally on iOS devices (acting as a local database wrapper)",
      "The main CPU processor of the iPhone",
      "An API for formatting JSON data"
    ],
    "correctIndex": 1,
    "explanation": "Core Data manages model objects, tracking relationships, undo actions, and persisting data locally (often backed by SQLite databases).",
    "difficulty": "moderate"
  },
  {
    "question": "What does the `@State` property wrapper do in SwiftUI?",
    "options": [
      "Saves data persistently to the cloud",
      "Declares a local source of truth for value types, causing SwiftUI to redraw the widget view whenever the value changes",
      "Checks the network connection status",
      "Encrypts local variable values"
    ],
    "correctIndex": 1,
    "explanation": "SwiftUI views are immutable structs. Marking a variable with `@State` tells the runtime to manage the value memory outside the struct, enabling updates and triggering redraws.",
    "difficulty": "moderate"
  },
  {
    "question": "What is the difference between `@ObservedObject`, `@StateObject`, and `@EnvironmentObject` in SwiftUI state management?",
    "options": [
      "ObservedObject is for mobile; EnvironmentObject is for web",
      "StateObject creates and owns the object instance (surviving view updates); ObservedObject references an existing object; EnvironmentObject retrieves shared objects from the environment",
      "EnvironmentObject is only used for databases",
      "They are identical in memory lifecycle"
    ],
    "correctIndex": 1,
    "explanation": "`@StateObject` instantiates a reference model (persistent). `@ObservedObject` is used when the model is passed down from a parent. `@EnvironmentObject` avoids manual prop drilling, accessing data injected at the root.",
    "difficulty": "moderate"
  },
  {
    "question": "What is the 'Keychain' in iOS, and when should it be used instead of UserDefaults?",
    "options": [
      "A key folder in file manager; used for layout assets",
      "A secure database for storing sensitive data (like passwords, keys, tokens); used instead of UserDefaults (which stores plaintext plist configurations)",
      "A cloud storage drive; used for backing up photos",
      "An encryption key generator; used to lock code files"
    ],
    "correctIndex": 1,
    "explanation": "UserDefaults stores basic settings in plaintext files easily read on jailbroken devices. Keychain encrypts data using hardware security modules, making it mandatory for API keys or user passwords.",
    "difficulty": "moderate"
  },
  {
    "question": "What does a 'Provisioning Profile' do in iOS development?",
    "options": [
      "Allocates CPU power to the application",
      "A profile containing certificates, app identifiers, and device IDs that authorizes the iOS device to run the compiled application during development",
      "Schedules updates on the App Store Connect portal",
      "Manages database schemas on iCloud servers"
    ],
    "correctIndex": 1,
    "explanation": "Provisioning profiles link development certificates to target devices (UUIDs), ensuring only authorized apps signed by certified developers install on test iPhones.",
    "difficulty": "moderate"
  },
  {
    "question": "What is 'Concurrency' using Swift's async/await (introduced in Swift 5.5) and how does it compare to GCD?",
    "options": [
      "Async/await is for databases; GCD is for files",
      "Swift Concurrency provides structured concurrency built natively into the language (using async/await, Tasks, and Actors), improving readability over GCD closure callbacks",
      "GCD is always faster and replaces async/await",
      "They are identical in thread allocation methods"
    ],
    "correctIndex": 1,
    "explanation": "Legacy GCD relies on nested closures (callback hell) prone to retain cycles. Swift's structured concurrency uses compiler checks, async/await code style, and Actors to prevent data races.",
    "difficulty": "hard"
  },
  {
    "question": "What are 'Actors' in Swift, and how do they resolve data race conditions in concurrent code?",
    "options": [
      "Objects representing game characters; resolve by checking animations",
      "Reference types that isolate their state and ensure only one thread can access their mutable properties at a time, protecting against data races",
      "Server nodes in Apple clusters; resolve by load balancing",
      "Database entities; resolve by primary keys"
    ],
    "correctIndex": 1,
    "explanation": "Actors isolate mutable state. The compiler enforces that any calls to read/write properties on an actor from the outside must be asynchronous (using `await`), synchronizing access to prevent concurrent mutations.",
    "difficulty": "hard"
  },
  {
    "question": "What is the difference between `weak` and `unowned` references in Swift, and when is `unowned` preferred?",
    "options": [
      "weak is for variables; unowned is for constants",
      "weak is optional and becomes nil when the instance is deallocated; unowned assumes the instance will never be nil during its lifetime, preferred when lifecycles are identical",
      "unowned is garbage collected faster than weak",
      "weak is only for structs; unowned for classes"
    ],
    "correctIndex": 1,
    "explanation": "`weak` variables are optional types (`weak var`). `unowned` is non-optional. Accessing an `unowned` reference after its target is deallocated triggers a runtime crash (equivalent to a null pointer exception), so it must be used with caution.",
    "difficulty": "hard"
  },
  {
    "question": "In iOS development, what is 'Method Dispatch' (Static, Table, Message) and how does Swift choose between them?",
    "options": [
      "Routing network traffic; Swift chooses by checking domain names",
      "The mechanism the program uses to locate executable code for a method call: Static (fastest, compile-time), Table (virtual dispatch), Message (slowest dynamic Cocoa dispatch)",
      "Method dispatch is configured in the App Store Connect portal",
      "Swift always uses Table dispatch for all classes"
    ],
    "correctIndex": 1,
    "explanation": "Swift optimizes. Value types (structs) use Static dispatch. Class methods use Table dispatch (vtable). Classes inheriting from NSObject or marked `@objc` support Message dispatch (dynamic lookup, required for KVO/CoreData).",
    "difficulty": "hard"
  },
  {
    "question": "What is 'App Thinning' (Slicing, Bitcode, On-Demand Resources) in iOS app distribution?",
    "options": [
      "Minifying the code lines inside Xcode projects",
      "Optimizations where Apple delivers only the asset resolution and executable architecture matching the user's specific iPhone model, reducing download sizes",
      "A style guide that keeps the UI layout slim",
      "A database compression algorithm"
    ],
    "correctIndex": 1,
    "explanation": "App thinning reduces download footprints. Slicing ensures an iPhone 13 doesn't download 3x image assets meant for iPad screens. On-Demand Resources load game levels dynamically as the user progresses.",
    "difficulty": "hard"
  }
]
